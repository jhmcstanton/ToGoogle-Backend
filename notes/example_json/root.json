# Request (only POST)

{
    'username': String,
    'password': String,    
}

# Response, array is arbitrarily long
[
    {
	'note_id' : Int
	'title': String,
	'creation_date': DateTime,
	'last_edit_date': DateTime,
    }	
]
